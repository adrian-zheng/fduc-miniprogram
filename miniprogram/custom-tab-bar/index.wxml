<!--custom-tab-bar/index.wxml-->
<van-tabbar active="{{ active }}" bind:change="onChange"
active-color="#b15373" inactive-color="#000" >
  <van-tabbar-item wx:for="{{ list }}" wx:key="name" name="{{item.name}}" icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>